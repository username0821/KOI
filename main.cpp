#include "stdio.h"

int main()
{
    int a, b, c, i, j;
    scanf("%d", &a);
    for(i = 1; i < a+1; i++)
    {
        if(a % 3)
        {
            printf("%d ", i);
        }
        else()
        {
            printf("X ");
        }
    }
     return 0;
}
